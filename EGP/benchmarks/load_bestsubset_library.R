library(bestsubset)
# Get information about the package
# package_path <- system.file(package = "mypackage")
# print(package_path)
# Get the installation path of the package
# package_path <- find.package("bestsubset")
# print(package_path)

set.seed(0)
s = sim.xy